<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.academy.user.mileage.MileageMapper">

  <resultMap id="LedgerResult" type="com.academy.user.mileage.MileageLedgerEntry">
    <constructor>
      <idArg column="ledger_id"     javaType="long"/>
      <arg   column="user_id"       javaType="String"/>
      <arg   column="delta"         javaType="java.math.BigDecimal"/>
      <arg   column="reason"        javaType="String"/>
      <arg   column="order_id"      javaType="String"/>
      <arg   column="balance_after" javaType="java.math.BigDecimal"/>
      <arg   column="created_at"    javaType="java.time.LocalDateTime"/>
    </constructor>
  </resultMap>

  <select id="sumBalance" resultType="java.math.BigDecimal">
    SELECT COALESCE(SUM(delta), 0) FROM pt_mileage_ledger WHERE user_id = #{userId}
  </select>

  <select id="findHistory" resultMap="LedgerResult">
    SELECT ledger_id, user_id, delta, reason, order_id, balance_after, created_at
      FROM pt_mileage_ledger
     WHERE user_id = #{userId}
     ORDER BY ledger_id DESC
     LIMIT #{limit}
  </select>

  <insert id="insertEntry">
    INSERT INTO pt_mileage_ledger (user_id, delta, reason, order_id, balance_after)
    VALUES (#{userId}, #{delta}, #{reason}, #{orderId}, #{balanceAfter})
  </insert>

</mapper>
