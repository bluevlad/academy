<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.academy.user.login.UserAccountMapper">

  <resultMap id="UserAccountResult" type="com.academy.user.login.UserAccountVO">
    <result property="userId"    column="user_id"/>
    <result property="userNm"    column="user_nm"/>
    <result property="userPwd"   column="user_pwd"/>
    <result property="userRole"  column="user_role"/>
    <result property="adminRole" column="admin_role"/>
    <result property="email"     column="email"/>
    <result property="isUse"     column="is_use"/>
    <result property="regDt"     column="reg_dt"/>
    <result property="updDt"     column="upd_dt"/>
  </resultMap>

  <select id="findByUserId" resultMap="UserAccountResult">
    SELECT user_id, user_nm, user_pwd, user_role, admin_role,
           email, is_use, reg_dt, upd_dt
      FROM acm_member
     WHERE user_id = #{userId}
     LIMIT 1
  </select>

  <update id="updatePassword">
    UPDATE acm_member
       SET user_pwd = #{passwordHash},
           upd_dt   = NOW()
     WHERE user_id  = #{userId}
  </update>

  <select id="existsByUserId" resultType="int">
    SELECT COUNT(*) FROM acm_member WHERE user_id = #{userId}
  </select>

  <select id="existsByEmail" resultType="int">
    SELECT COUNT(*) FROM acm_member WHERE email = #{email}
  </select>

  <insert id="insert" parameterType="com.academy.user.login.UserAccountVO">
    INSERT INTO acm_member (
      user_id, user_nm, user_pwd, user_role, email, is_use,
      reg_dt, reg_id, upd_dt, upd_id
    ) VALUES (
      #{userId}, #{userNm}, #{userPwd},
      COALESCE(#{userRole}, 'USER'), #{email},
      COALESCE(#{isUse}, 'Y'),
      NOW(), #{userId}, NOW(), #{userId}
    )
  </insert>

  <update id="updateProfile" parameterType="com.academy.user.login.UserAccountVO">
    UPDATE acm_member
       SET user_nm = #{userNm},
           email   = #{email},
           upd_dt  = NOW(),
           upd_id  = #{userId}
     WHERE user_id = #{userId}
  </update>

  <update id="markWithdrawn">
    UPDATE acm_member
       SET is_use = 'N',
           upd_dt = NOW(),
           upd_id = #{userId}
     WHERE user_id = #{userId}
  </update>

</mapper>
